# TASK-009 Print suggested git add and commit command after init-project

## Status

done

## Description

After `make init-project` generates all files, the user still needs to figure out
what to stage and commit. Print the exact commands to run so the user can copy-paste
them without thinking.

## Acceptance criteria

- [x] After successful generation, `init-project` prints the files to stage and a
  suggested commit command
- [x] The suggested commands cover all files generated by `init-project`:
  `CLAUDE.md`, `pyproject.toml`, `.github/`, `.claude/`
- [x] CHANGELOG.md updated

## Branch

**Branch name:** `task/009-init-project-suggest-commit`
**Switch/create:** `git checkout -b task/009-init-project-suggest-commit`
**Make target:** `make branch-task f=TASK-009`

## Completion

**Date:** 2026-04-30
**Summary:** Added a closing echo block to `init-project` that prints the suggested
git add and commit commands after successful generation.
**Files changed:**
- `Makefile` — echo block added to `init-project`
- `CHANGELOG.md` — entry added
**Branch:** `git checkout task/009-init-project-suggest-commit`
**Stage:** `git add Makefile CHANGELOG.md docs/tasks/TASK-009-init-project-suggest-commit.md`
**Commit:** `git commit -m "Print suggested git commands after init-project"`
