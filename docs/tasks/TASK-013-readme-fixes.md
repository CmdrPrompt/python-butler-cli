# TASK-013 Fix and update README

## Status

done

## Description

Several issues found during review:

1. `sed -i` in "Adopting in an existing project" is macOS-incompatible — requires
   `sed -i ''` on macOS (GNU sed vs BSD sed).
2. "What's included" lists scaffold as "e.g. `pyproject.toml`" but now also includes
   `.gitignore.tmpl` and `.pre-commit-config.yaml.tmpl`.
3. "Governance files" section only mentions `CLAUDE.md` and agent files — does not
   mention `pyproject.toml`, `.gitignore`, and `.pre-commit-config.yaml` which are
   also generated by `init-project`.
4. "Adopting in an existing project" is missing commit and push steps (added to
   new project flow in TASK-012 but not to existing project flow).

## Acceptance criteria

- [x] `sed -i` replaced with a cross-platform alternative in the existing project
  adoption guide
- [x] "What's included" scaffold description updated to reflect current scaffold files
- [x] "Governance files" section lists all files generated by `init-project`
- [x] "Adopting in an existing project" includes commit and push steps
- [x] CHANGELOG.md updated

## Branch

**Branch name:** `task/013-readme-fixes`
**Switch/create:** `git checkout -b task/013-readme-fixes`
**Make target:** `make branch-task f=TASK-013`

## Completion

**Date:** 2026-04-30
**Summary:** Fixed sed -i macOS incompatibility, updated scaffold description,
updated governance files section, and added commit/push steps to existing project flow.
**Files changed:**
- `README.md` — four fixes applied
- `CHANGELOG.md` — entry added
**Branch:** `git checkout task/013-readme-fixes`
**Stage:** `git add README.md CHANGELOG.md docs/tasks/TASK-013-readme-fixes.md`
**Commit:** `git commit -m "Fix and update README"`
